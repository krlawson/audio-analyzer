name: Fast Audio Analysis
on:
  push:
    paths:
      - 'uploads/**' # Runs only when you add music

permissions:
  contents: write
  packages: read

jobs:
  analyze:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/${{ github.repository }}/forensic-studio:latest
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }} # The 'Handshake'
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Forensic Analysis
        run: python analyzer.py

      - name: Save Results to Repo
        run: |
          # 1. THE GEOGRAPHY FIX
          git config --global --add safe.directory /__w/${{ github.event.repository.name }}/${{ github.event.repository.name }}
          
          # 2. Identity
          git config --local user.name "GitHub Action"
          git config --local user.email "action@github.com"
          
          # 3. THE FIX: Add and Commit BEFORE pulling
          # This clears the 'Unstaged Changes' error by packaging the results first
          git add "map_*.png" "data_*.json" "processed/*"
          
          # Only commit if there are actually new files to save
          git diff --staged --quiet || git commit -m "Forensic Batch Update [skip ci]"

          # 4. Now we can safely pull and push
          git pull origin main --rebase
          git push










